import numpy as np
import matplotlib.pyplot as plt

""" Question 2 is going to be about generating a plot. """

a = 2
t = np.linspace(0, 5, num=200)
f = np.exp(-a*t)*(np.sin(5 * t) + np.cos(10 * t))


plt.plot(t, f)
plt.title('29th May 2021')
plt.xlabel('x')
plt.ylabel('y')
plt.show()
